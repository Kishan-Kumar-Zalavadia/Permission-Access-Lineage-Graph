spring.application.name=access-graph

server.port=${PORT:8080}

# Never hardcode credentials — always read from environment variables.
# Set these in your shell, a .env loaded by your process manager, or your
# hosting provider's environment/secrets configuration.
cognodb.uri=${COGNODB_URI}
cognodb.username=${COGNODB_USER}
cognodb.password=${COGNODB_PASSWORD}
